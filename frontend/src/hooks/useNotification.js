// Re-export useNotification from NotificationContext — this is the canonical import path.
// Usage: import { useNotification } from '../hooks/useNotification';
export { useNotification } from '../context/NotificationContext';
